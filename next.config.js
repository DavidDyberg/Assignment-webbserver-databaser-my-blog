/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["media.wired.com", "rifrhcafcpywicolsecd.supabase.co"],
  },
};

module.exports = nextConfig;
